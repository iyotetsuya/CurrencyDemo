<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">


        <EditText
            android:id="@+id/number_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="@string/put_number_here"
            android:importantForAutofill="no"
            android:inputType="numberDecimal"
            android:text="@{model.inputValue}"
            android:labelFor="@id/selected_currency"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/selected_currency" />

        <TextView
            android:id="@+id/selected_currency"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{@string/selected_currency(currencyCode)}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="@string/selected_currency" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/quote_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:clipToPadding="false"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/number_input"
            tools:listitem="@layout/rate_item" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <data>

        <import type="iyotetsuya.currencyconversion.vo.Currency" />


        <variable
            name="currencyCode"
            type="String" />

        <variable
            name="model"
            type="iyotetsuya.currencyconversion.ui.currency.InputViewModel"/>


    </data>


</layout>
